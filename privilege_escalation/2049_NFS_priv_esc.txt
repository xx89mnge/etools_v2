# NFS PrivEsc (2049)
showmount -e <target>; rpcinfo -p <target>                     # Exports/RPC
mount -t nfs <target>:/<export> /mnt/nfs -o vers=3             # تركيب
touch /mnt/nfs/test.txt                                        # تحقق كتابة
# no_root_squash → أنشئ SUID ثم انسخه إلى الـ export
# بدائل: authorized_keys، cron داخل الـ export
sudo -l; find / -perm -4000 -type f 2>/dev/null; getcap -r / 2>/dev/null
