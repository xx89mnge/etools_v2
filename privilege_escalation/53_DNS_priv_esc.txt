# DNS PrivEsc (53)
cat /etc/named.conf /etc/bind/named.conf 2>/dev/null     # تحقق من مسارات zones القابلة للكتابة
find /var/named -writable -type f 2>/dev/null            # ملفات zone قابلة للكتابة (خطر عالي)
ps aux | grep -i named                                   # تشغيل المستخدم/المجموعة للخدمة
sudo -l                                                  # sudo
find / -perm -4000 -type f 2>/dev/null                   # SUID
getcap -r / 2>/dev/null                                  # قدرات
grep -R "include|script" /etc/named* 2>/dev/null       # سكربتات/إدراج قد يُنفّذ
wget https://.../linpeas.sh; chmod +x linpeas.sh; ./linpeas.sh
